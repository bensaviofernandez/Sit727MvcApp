apiVersion: v1
kind: Service
metadata:
  name: sit727-mvc-service
spec:
  type: NodePort
  selector:
    app: sit727-mvc-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
